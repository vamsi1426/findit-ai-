rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SECURITY RULE: Allow read/write only if the user is logged in
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
